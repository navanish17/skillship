// File:    frontend/src/hooks/useDashboard.ts
// Purpose: Hook that fetches the role-appropriate dashboard payload via React Query.
// Owner:   Pranav
// TODO:    useQuery({ queryKey:['dashboard', role], queryFn: () => api.get(`/dashboards/${role}`) }).
