<!--
File:    frontend/src/components/README.md
Purpose: Folder for shared UI components (Button, Card, Table, Chart, QuizPlayer, ChatDrawer, ...).
Owner:   Pranav
-->

# Shared components

Add one folder (or file) per reusable component here. Keep page-specific components inside their
page folder.

Planned components (scaffold as needed):
- `Button.tsx`, `Input.tsx`, `Card.tsx`, `Table.tsx`
- `KPICard.tsx`, `TrendChart.tsx`, `RiskBadge.tsx`
- `QuizPlayer.tsx`, `QuestionEditor.tsx`
- `TutorChat.tsx`, `CareerPilotChat.tsx`
- `NotificationBell.tsx`
