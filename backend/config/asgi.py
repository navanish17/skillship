"""
File:    backend/config/asgi.py
Purpose: ASGI application entrypoint (for async servers like Daphne / Uvicorn).
Why:     Required if we later add WebSockets (live chat, real-time notifications).
Owner:   Navanish
TODO:    Standard Django ASGI boilerplate.
"""
